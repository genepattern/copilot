Directly accessing Broad filesystem in GenePattern requires server administrator intervention: files located in specific Broad internal directories can be made accessible by adding the directory path to the server configuration files under 'server.browse.file.system.root' settings, which must be readable by the GP server role account, with paths configurable at default, group, or individual user levels.