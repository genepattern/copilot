In this GenePattern forum thread, a user asks how to programmatically reference custom.properties definitions for tools like samtools and tabix when creating new modules, with a response detailing two methods: (1) adding a specific entry in custom.properties with a relative path reference, or (2) directly using the path defined in custom.properties on the command line, enabling flexible management of multiple tool versions within the GenePattern environment.